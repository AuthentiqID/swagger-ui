<div class='heading'>
  <h2 id="{{anchorId}}">
    <a href='#!/{{{sanitize id}}}' class="toggleEndpointList text-primary" data-id="{{{sanitize id}}}">{{{sanitize name}}}</a> {{#summary}} : {{/summary}}{{{sanitize summary}}}
  </h2>
</div>
<ul class='endpoints list-unstyled' id='{{{sanitize id}}}_endpoint_list'>

</ul>
<hr />
