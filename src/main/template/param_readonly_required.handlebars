<td class='code required'>
	<label for='{{valueId}}'>{{name}}</label>
	<span class="glyphicon glyphicon glyphicon-asterisk text-primary" aria-hidden="true"></span>
</td>
{{!--
<td>
    {{#if isBody}}
        <textarea class='body-textarea form-control' readonly='readonly' placeholder='(required)' name='{{name}}' id='{{valueId}}'>{{default}}</textarea>
    {{else}}
        {{#if default}}
            {{default}}
        {{else}}
            (empty)
        {{/if}}
    {{/if}}
</td>
--}}
<td class="markdown">{{{description}}}</td>
<td>{{{paramType}}}</td>
<td><span class="model-signature"></span></td>
